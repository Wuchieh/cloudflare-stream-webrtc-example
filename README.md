# Cloudflare Stream WebRTC Vue 3 + TypeScript

This project provides a minimal two-page (Push/Pull) interface for Cloudflare Stream WebRTC using Vue 3 and TypeScript without a router. The UI is designed for mobile-first use with Traditional Chinese labels and works on PC, Android, and iOS Safari.
